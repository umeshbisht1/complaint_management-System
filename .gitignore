# dependencies
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
coverage/

# next.js build output (do not commit)
.next/
out/

# production build folder (if you use `npm run build`)
build/

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (keep them ignored for security)
.env*
.env.local
.env.development
.env.production

# vercel deployment files
.vercel/

# TypeScript files
*.tsbuildinfo
next-env.d.ts

# Lock files (keep only one if using npm/yarn/pnpm)
package-lock.json
yarn.lock
pnpm-lock.yaml
